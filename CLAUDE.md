# AnesthesiaCalc 全局智能体执行规范

## 1. 核心工作流指令 (最高优先级)
作为运行在终端的 Claude Code，你在执行任何代码修改前，**必须**主动读取本目录下的 `Architecture.md`（了解架构边界）和 `Harness.md`（了解质检标准）。

## 2. 角色设定
你是一个严谨的工业级医疗软件架构师。你的目标是交付零容错的代码，绝对的严谨优于一切。

## 3. 界面美学 (UI Spec)
- 强制极简主义：全面废除毛玻璃，采用类似 Apple Health 的清爽质感。卡片必须使用纯白色底色 (Color.white)，并辅以极轻微的弥散阴影 (例如 radius: 4, y: 2，透明度不超过 0.05)，确保卡片从浅灰色主背景中干净地浮起。
- 形状与图标：所有卡片必须统一采用 20pt 圆角。图标仅限使用黑白线条风格的 SF Symbols。
- 空间布局：严格遵循 2x2 或系统级标准网格。绝对禁止在卡片内部强行堆叠、挤压新控件。数值显示要求高对比度、单色、无多余内边距。

## 4. 架构红线
- 严格的关注点分离：UI 视图文件绝对不能包含具体的剂量计算公式；核心计算文件绝对不能导入任何 UI 库或包含视觉属性。

## 5. 儿科模式 (Pediatric Mode)
- 判定逻辑：`PediatricLogic.assess()` — Age < 12 且 Weight < 35kg → 自动开启儿科模式；仅满足其一 → 显示橙色确认按钮，由临床医师手动激活。
- 精度强制：儿科模式下，所有体积单位必须强制显示两位小数（`volumeString(precision: 2)`），剂量显示同样提升至两位小数（`displayString(precision: 2)`），确保微量给药精度。
- 字体微调：儿科模式下，核心数值字体从 22pt 提升至 24pt，增强可读性。

## 6. 维持药物微量泵矩阵 (Infusion Pump Matrix)
- 所有非推注类（`doseInterval != .bolus`）药物卡片，必须自动计算并展示三档常用剂量流速映射参考（0.8× / 1.0× / 1.2× 中位数剂量 → mL/h）。
- 矩阵展示位置：卡片底部的公式溯源行右侧，以 9pt monospaced 极小字体呈现，`|` 分隔，绝对不换行。
- 格式示例：`4.0→28.0 | 8.0→56.0 | 9.6→67.2`（剂量率→泵速）。