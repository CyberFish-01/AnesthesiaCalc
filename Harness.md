# 自动化评估与熔断机制 (Harness & Circuit Breaker)

在任何代码修改完成后，你必须在后台运行以下自我审查。只有全部通过，才能向用户汇报任务完成：

## Check 1: 物理隔离测试 (Separation Check)
- 扫描修改过的文件。如果发现 `View` 结尾的文件中出现了具体的数学加减乘除业务公式，或 `Calculator` 文件中出现了界面排版代码，测试失败，必须立刻重写分离。

## Check 2: 幻觉阻断测试 (Anti-Hallucination Lock)
- 所有的麻醉药量计算，必须严格收敛于最通用、最普适的基础因子（强制基于体重等基础指标的通用算法）。
- 严禁为了显得“智能”而自行推演、编造或引入未经用户明确授权的复杂患者特异性变量。若检测到脱离基础体重的动态计算逻辑，测试直接判负。

## Check 3: 熔断机制 (Doom Loop Breaker)
- 【最高防线指令】如果同一模块的代码重构连续 3 次未能达成目标，或编译后依然存在之前的报错：
  1. 立即停止一切写代码和打补丁的行为。
  2. 强制将代码回滚 (Revert) 到上一个稳定的版本。
  3. 停止运行，并在终端输出一份《逻辑崩塌分析报告》，等待人类节点重新指示。

## Check 5: 视觉防截断与空间防卫 (Anti-Truncation & Layout Priority)
- 强制规则 1：横向布局 (HStack) 中，左侧核心身份文本（如药品名）必须使用 `.layoutPriority(1)` 或 `.fixedSize`，强制抢占空间，绝对禁止被右侧大字体挤压。
- 强制规则 2：UI 层绝对禁止出现非预期的省略号 (...) 截断。每次 UI 改动后必须目视检查所有药品名称、浓度标签是否完整显示。
- 强制规则 3：字符串剥离规则必须具备鲁棒性，同时处理半角括号 `" ("` 和全角括号 `"（"`，确保无论数据源格式如何，均不会在卡片内展示冗余英文后缀。

## Check 6: 逻辑透明化 (Formula Traceability)
- 强制规则 1：所有药品卡片必须展示核心计算路径的溯源公式（例如：`剂量 = 70.0kg × 2.0mg/kg`），使临床医生可以肉眼进行二次心算复核。
- 强制规则 2：公式必须在 Core 层由 `DrugDoseRange.formulaString` 统一生成，UI 层仅负责展示，禁止在 View 中手动拼接公式字符串。
- 强制规则 3：对于固定剂量（minMultiplier == maxMultiplier）显示单值公式，对于区间剂量显示范围公式。

## Check 8: 自动路由透明化 (Auto-Route Transparency)
- 强制规则 1：当 RiskEngine 自动将权重基准偏离实际体重 (TBW) 时，UI 必须在卡片右侧支撑信息中以系统蓝色高亮显示当前使用的权重基准。
- 强制规则 2：高亮必须伴随 `sparkles` 智能图标（SF Symbol），确保医生在 0.5 秒内识别出系统已触发自动优化路由。
- 强制规则 3：公式溯源（`formulaString`）中的体重数值必须与自动路由后实际使用的体重完全对齐，杜绝展示 TBW 但计算公式使用 IBW 的不一致。
- 强制规则 4：自动路由逻辑必须集中在 Core 层 `RiskEngine.resolveWeightBase()` 单一路径，禁止 UI 层或 DrugCalculator 中存在分散的、重复的或硬编码的权重判断。

## Check 9: 知识合规性与 RAG 溯源 (Knowledge Compliance & RAG Traceability)
- 强制规则 1：所有 AI 生成或指南驱动的临床建议，必须明确标注出处（指南全称 + 页码），禁止无来源的笼统建议。
- 强制规则 2：严禁提供任何未经公开发表指南或系统性综述证实的"经验性"给药建议。所有指南条目必须可追溯到具体文献。
- 强制规则 3：每次 AI/指南响应末尾必须强制附加免责声明：**"本建议仅供临床参考，最终决策由值班医师负责。"**
- 强制规则 4：引用溯源 UI 组件（橙色 `doc.text.magnifyingglass` 标签）必须紧跟在每条 AI 回复下方，确保来源与建议不分离。
- 强制规则 5：Mock RAG 检索过程必须可视化（显示"正在检索知识库..."），防止医师在检索未完成时误以为无响应。

## Check 10: 知识层级与冲突判定 (Knowledge Hierarchy & Conflict Resolution)
- 强制规则 1：临床操作建议（剂量、风险、禁忌）必须以最新版《中国麻醉学指南》或 ASA/ESAIC 国际指南为准，引用必须标注发布年份和机构全称。
- 强制规则 2：药理机制与病理生理描述优先参考《米勒麻醉学》(Miller's Anesthesia)，引用必须标注版本与章节。
- 强制规则 3：冲突判定 — 若指南结论与米勒机制描述存在不一致，必须同时列出两者，并以橙色 `exclamationmark.shield.fill` 横幅高亮提示，注明"以上方最新指南建议为准"。
- 强制规则 4：查询路由 — KnowledgeProvider.QueryClassifier 必须根据查询意图自动路由：临床关键词（剂量、风险、推荐）→ 指南优先；机制关键词（为什么、机制、分布）→ 米勒优先。
- 强制规则 5：双源引用组件视觉差异 — 指南卡片使用蓝色系（`accentColor` + 年份胶囊标签），米勒卡片使用棕色系（`brown` + 斜体书名 + `text.book.closed` 图标），确保一目可区分来源层级。

## Check 11: 纵深信息呈现 (Deep Dive Information Presentation)
- 强制规则 1：长按药品卡片触发的 Deep Dive 视图必须采用半屏抽屉风格（`presentationDetents([.medium, .large])`），不得全屏覆盖主计算界面，确保医生可随时返回主页面。
- 强制规则 2：Deep Dive 视图内必须严格分为两个视觉独立的分区 — 药理机制 (PK/PD) 与临床指南红线，分别使用棕色系和红色警告色系进行视觉区分，不允许混排。
- 强制规则 3：所有内容必须采用要点式 (Bullet-point) 短句排版，每条不超过两行，绝对禁止长篇大论的段落文字。
- 强制规则 4：每个分区必须在标题栏明确标注知识来源（药理机制→"米勒麻醉学 第九版"，临床指南→"最新麻醉学指南"），来源标签不得与内容分离。
- 强制规则 5：长按触发时必须伴有 `UIImpactFeedbackGenerator(style: .medium)` 物理震动反馈，确保医生在触觉上感知到纵深信息的展开。

## Check 12: 医疗数据绝对准确性与全链路统一性 (Medical Data Integrity)
- 强制规则 1：溯源与自检 — 数据库中所有药物参数（浓度、极量、禁忌症）必须与《米勒麻醉学》及现行权威指南（CSA/ASA/ESAIC）进行逻辑自检。每次数据录入后必须运行全量校验脚本，确认无字段遗漏或数值越界。
- 强制规则 2：强制中文化 — 所有内部匹配规则、判断条件反馈、UI 展示标签及红线预警，必须 100% 采用准确的医学中文表述。严禁在面向临床医师的界面中残留英文标识（如 "induction"、"perHour"），代码内部标识除外。
- 强制规则 3：完整映射 — 数据库中的每一个字段（pkpd、redLines、category、concentration、dosageRules）必须在首页 UniversalDrugCardView 或 Deep Dive 视图中有明确的中文 UI 元素对应。严禁存在"后台有数据、前台无法显示"的孤岛信息。
- 强制规则 4：药物清单全量覆盖 — DrugManager 必须加载全部已录入药物，设置页药物清单与首页展示的药物集合必须同源一致，不得出现"设置页可见、首页不可见"的断链。
- 强制规则 5：分类完整性 — 所有药物必须归属到一个中文药理学分类（镇静催眠药、阿片类镇痛药、神经肌肉阻滞药、血管活性药等），且该分类必须在首页 UI 中以中文标题形式可见。

## Check 13: 纵深信息呈现 (Deep Dive UI) 规范
- 药物的深度信息视图内的知识来源，必须有明确的视觉分隔（理论 vs 指南）。
- 绝对禁止包含长篇大论的散文式文本，必须采用要点式 (Bullet-point) 排版。

## Check 14: 医疗数据绝对准确性与全链路统一性 (Medical Data Integrity)
- 数据库中所有的药物参数、浓度、禁忌症必须与现行权威指南逻辑自洽。
- 所有前台 UI 映射必须 100% 采用医学中文表述。严禁存在"后台有数据、前台无对应 UI 渲染"的孤岛信息。

## Check 15: 零容忍数学安全防线 (P0 Math Safety)
- **严防除零炸弹**：所有涉及微量泵速率和容积计算的代码，必须对浓度参数进行强制防御 (`guard concentrationMgPerMl > 0`)。绝对禁止产生 `inf` (无穷大) 或 `nan`。

## Check 16: 输入边界与认知防呆 (Input Validation)
- **禁止静默保留**：当患者的体征输入框被清空或输入无效字符时，系统底层状态必须同步置为 `nil` 或 `0`，并触发 UI 拦截。绝对禁止后台隐蔽使用旧数据。

## Check 17: 新模块独立隔离 (New Module Isolation)
- 强制规则 1：气道/ABL/液体/预案/ABG 五个新模块的 Core 引擎必须为独立枚举，每次调用接收全新 Input struct，不持有任何可变状态。
- 强制规则 2：新模块 UI 视图必须通过 `ClinicalContext.shared` 获取患者数据，不得自行维护独立的患者状态变量。
- 强制规则 3：新模块之间不得互相引用——每个模块是完全自包含的计算单元。

## Check 18: 紧急预案剂量正确性 (Emergency Protocol Dosage Correctness)
- 强制规则 1：体重依赖的预计算剂量必须基于 `EmergencyProtocolEngine.generateAll(for:)` 接收的 weightKg 参数，不得硬编码固定剂量在 UI 层。
- 强制规则 2：预案中药物的计算剂量与实际给药指导 (rawInstruction) 必须一致——如氨甲环酸统一为固定 1g，丹曲林为 2.5mg/kg。
- 强制规则 3：预案的识别要点、立即措施、用药步骤、升级措施四部分必须全部为非空数组。

## Check 19: ABG 分析公式正确性 (ABG Formula Correctness)
- 强制规则 1：Winter 公式实现必须为 `1.5 × HCO₃ + 8 ± 2`。
- 强制规则 2：A-aDO₂ 公式必须为 `(PB - 47) × FIO₂ - PaCO₂ / 0.8 - PaO₂`。
- 强制规则 3：AG 公式必须为 `Na⁺ - Cl⁻ - HCO₃⁻`。
- 强制规则 4：乳酸风险分层必须严格对应 <2.0 / 2.0-4.0 / 4.0-8.0 / >8.0。